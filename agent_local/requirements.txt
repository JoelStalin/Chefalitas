# Core WebSocket server
websockets>=12.0,<14.0

# Windows printing (win32print) and Windows API access
pywin32>=306

# GUI tray/controller icon support (if you usas pystray + Pillow)
pystray>=0.19.5
Pillow>=10.0.0

# Packaging (optional, for building .exe)
pyinstaller>=6.0
